<div class="container-fluid">
  <div class="page-header"><h3>Select recipe to add ingredients</h3></div>
  <ul class="list-group">
    <% @recipes.each do |recipe| %>
      <li class="list-group-item"><%= link_to recipe.name, recipe_path(recipe) %></li>
    <% end %>
  </ul>
</div>
