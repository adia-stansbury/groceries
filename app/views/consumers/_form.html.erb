<%= form_for @consumer do |f| %>
  <form class="form-inline">
    <div class="form-group">
      <%= f.label(:name) %>
      <%= f.text_field(:name, class:"form-control") %>
    </div>
    <%= f.submit('Add Consumer', class:"btn btn-success") %>
  </form>
<% end %>
